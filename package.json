{
  "name": "xlsx-to-json-lang-files",
  "version": "1.0.0",
  "description": "convert language translation in xlsx format into json file for each language",
  "main": "./lib/xlsx-to-json-lang-files.js",
  "scripts": {
    "test": "jest",
    "test-watch": "jest --watch *.js"
  },
  "author": "Gendy",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "json-format": "^1.0.1",
    "xlsx": "^0.14.2"
  },
  "devDependencies": {
    "jest": "^24.7.1"
  }
}
